#include "kern/syscall_code.h"

SYSCALL_FUNC(create, SYS_CREATE);

SYSCALL_FUNC(myTid, SYS_TID);

SYSCALL_FUNC(myParentTid, SYS_PARENT_TID);

SYSCALL_FUNC(yield, SYS_YIELD);

SYSCALL_FUNC(exit, SYS_EXIT);

SYSCALL_FUNC(send, SYS_SEND);

SYSCALL_FUNC(receive, SYS_RECEIVE);

SYSCALL_FUNC(reply, SYS_REPLY);

SYSCALL_FUNC(awaitEvent, SYS_AWAIT_EVENT);

SYSCALL_FUNC(shutdown, SYS_SHUTDOWN);

SYSCALL_FUNC(getIdleTime, SYS_IDLE_TIME);

